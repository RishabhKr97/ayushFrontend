import { DrNameFilterPipe } from './dr-name-filter.pipe';

describe('DrNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DrNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
